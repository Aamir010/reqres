name: Build
on:
  push:
    branches:
      - "master"

pull_request:
  branches:
    - main
  types: [opened, synchronize, closed]

jobs:
  test:
    name: Build image
    runs-on: ubuntu-latest

    steps:

    - name: Check out code
      run: |
        echo "First Test"